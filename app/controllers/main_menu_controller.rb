# frozen_string_literal: true

class MainMenuController < ApplicationController
  def index;
    # If condition is met: redirect_to servers_path
    # Not sure if this will end up being in MainMenuController or not
  end
end
